node_version = "18"

[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.build]
cmds = [
  "npm install",
  "npm run build",
  "cd server && npm install"
]

[start]
cmd = "cd server && NODE_ENV=production node server.js"
