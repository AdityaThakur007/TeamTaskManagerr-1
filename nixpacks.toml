[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "npm run build",
  "cd server && npm install"
]

[start]
cmd = "cd server && NODE_ENV=production node server.js"
